/*import React from 'react';
import { Image } from 'react-native';
import styles from '../styles/index.js';
import values from '../../const/index';

export default fuctions = {
    imageUser: (boolvalue) => {
        if (boolvalue) return (
            <Image
                source={require('../../assets/image/user-profile.jpg')}
                style={styles.stylesDrawer.image} />
        )
        else return null
    },

    imageBackgroud: (boolvalue) => {
        if (boolvalue) return require('../../assets/image/menu-bg.jpeg')
        else return require('../../assets/image/background.jpg')
    },

    textName: (boolvalue, text) => {
        if (boolvalue) return text
        else return ""
    },

    navigationFunction: (navigation,screenName) => {
        navigation.navigate(screenName)
    },

    typeList: (boolvalue) => {
        if(boolvalue) return values.drawerComponent.isloged
        else return values.drawerComponent.isnotloged
    }
}
*/